import 'package:flutter/material.dart';
import 'package:teamfix/View/LogIn/Body.dart';

class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
      // endDrawer: Drawer(),
    );
  }
}
